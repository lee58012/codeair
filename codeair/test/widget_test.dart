import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CodeAir app smoke test', (WidgetTester tester) async {
    // Firebase 초기화가 필요하므로 기본 스모크 테스트만 유지
    expect(true, isTrue);
  });
}
